window.addEventListener('load',()=>{
const form = document;
querySelector('#task-id')

const input= document;
querySelector
("#new-input-task")
console.log(input);
const tasks_List= document.querySelector('.task');
console.log(tasks_List);
})

form.addEventListener('submit', ()=>{
    console.log(input.value);

})
